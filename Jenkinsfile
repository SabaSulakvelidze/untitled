pipeline {
  agent any
  stages {
    stage('Check maven version') {
      steps {
        sh 'mvn --version'
      }
    }

    stage('error') {
      steps {
        echo 'Finito'
      }
    }

  }
}