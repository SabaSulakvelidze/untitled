pipeline {
  agent any
  stages {
    stage('Check Maven Version') {
      steps {
        sh 'sh mvn --version'
      }
    }

    stage('Status Check') {
      steps {
        echo 'Finito'
      }
    }

  }
}