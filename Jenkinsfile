pipeline {
  agent any
  stages {
    stage('Check Maven Version') {
      steps {
        sh 'git mvn --version'
      }
    }

    stage('Status Check') {
      steps {
        echo 'Finito'
      }
    }

  }
}